import React, { Component } from 'react';
import BasicLayout from '@/layouts/BasicLayout';

class PremsAdd extends Component {
  render() {
    return (
      <BasicLayout history={this.props.history}>
        <div>PremsAdd</div>
      </BasicLayout>
    );
  }
}

export default PremsAdd;
